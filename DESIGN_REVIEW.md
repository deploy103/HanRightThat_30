# 디자인 리뷰 — 소개 페이지(HANWOL-INTRO-V1)

작성 기준: 2026-09-18. 이 문서는 실제로 접근해 확인한 레퍼런스와, 그로부터 한빛제 소개 페이지(`src/landing/*`)에
반영/보류한 결정을 기록한다. 템플릿의 문구·사진·로고를 그대로 복제하지 않고, 레이아웃 원리와 정보 전달
순서만 참고했다.

## 1. 실제 확인한 참고 URL

| URL | 접근 결과 |
| --- | --- |
| https://dothack.io/ (.HACK 2026) | 접근 성공 — 아래 §2 참고 |
| https://www.seouljazz.co.kr/ (서울재즈페스티벌) | 접근 성공 — 아래 §2 참고 |
| https://imweb.me/theme/ , https://imweb.me/theme | **접근 실패 (HTTP 403)** — 두 경로 모두 자동화된 요청을 차단한다. 브라우저가 아닌
이 세션의 fetch 도구로는 열람할 수 없었다. 이 문서에서 imweb 템플릿을 "확인했다"고 서술하지 않는다.

## 2. 참고한 구체적 요소와 한빛제 적용

### .HACK 2026 (dothack.io)
- 히어로 최상단에 "날짜(요일 포함) · 장소"를 한 줄로 먼저 보여주고, 그다음 슬로건을 배치한다.
  → `LandingHero`의 `hero-facts`(일시/장소)를 슬라이드 타이틀 위·아래가 아니라 슬라이드와 나란히
  항상 보이는 고정 정보로 배치해 "지금 이 행사가 언제/어디서 열리는지"가 슬라이드가 넘어가도
  사라지지 않게 했다 — dothack.io는 날짜/장소를 배경 이미지에 섞어 넣어 반응형에서 잘릴 위험이
  있었는데, 한빛제는 별도 텍스트 블록(`.hero-fact`)으로 분리해 모바일에서도 줄바꿈이 깨지지 않게 했다.
- 프로그램 소개를 아이콘형 카드 6개로 나열하는 구조 → 한빛제는 부스/공연처럼 개수가 가변적인
  콘텐츠에는 카드 그리드 대신 가로 캐러셀(`LandingBooths`)을 썼다. dothack.io처럼 카드 개수가
  고정된 프로그램 소개(6개)와 달리, 부스는 8개 이상으로 늘어날 수 있어 그리드 하나로 고정하면
  세로로 한없이 길어지는 문제가 있었기 때문이다.
- 오시는 길을 지도+교통 안내로 별도 섹션화 → 한빛제 `LandingInfo`의 "장소" 블록과 `directionsUrl`
  링크로 축소 적용했다(지도 임베드는 요구사항상 필수가 아니라서 넣지 않았다).
- **적용하지 않은 것**: 진입 시 모달 팝업 2건, 참가 후기 인용구 카드, 스폰서 등급별 로고. 한빛제는
  이런 콘텐츠(연사 모집 공고, 등급별 후원사)가 실제로 없고, 요구사항이 "근거 없는 후원사·후기를
  만들지 않는다"고 명시하고 있어 그대로 제외했다.

### 서울재즈페스티벌
- **반면교사로 참고**: 타임테이블이 첫 화면/전용 섹션이 아니라 공지사항 게시글의 첨부 이미지로만
  제공된다. 텍스트가 아닌 이미지이므로 검색·스크린리더·모바일 확대에 취약하다.
  → 한빛제 `LandingSchedule`은 정반대로 설계했다: `scheduleItems`를 실제 HTML `<ol>` 타임라인으로
  렌더링해 텍스트 선택·스크린리더 탐색·모바일 줄바꿈이 모두 가능하게 했다. 이번 세션에서 "이미지로
  공지하면 끝"이라는 패턴을 따르지 않기로 한 근거가 이 리서치다.
- 상단 배너가 슬라이드로 순환되는 구조 → 한빛제 히어로 슬라이드(`useAutoRotate`)와 유사하지만,
  서울재즈는 정지/재생 컨트롤이 텍스트로 확인되지 않아(접근성 불명) 한빛제는 항상 정지 버튼을
  노출하고 `prefers-reduced-motion`에서 자동 재생을 끄는 쪽을 택했다(요구사항 §8 직결).

### imweb.me/theme
접근하지 못했으므로 imweb의 구체적 레이아웃은 이번 리뷰에 반영하지 않았다. 대신 위 두 실제 행사
사이트와 기존 `요구사항2.md` §5(.HACK/서울재즈/서울스프링/DebConf24 서술)를 근거로 삼았다.

## 3. 현재 화면에서 확인한 문제와 조치

리뷰 시작 시점(이 세션 이전, 미커밋 상태)의 `src/landing/*` 구현을 코드 레벨로 읽고 실제 API 응답
데이터(`data/festival.json`의 게시된 소개 콘텐츠)로 대조한 결과 다음 문제를 확인해 고쳤다.

1. **"전체 일정 보기" 동선 누락** — 요구사항은 히어로에 "축제 들어가기 / 전체 일정 보기 / 공연 순서
   보기" 3개 버튼을 요구하는데 실제 코드에는 2개(축제 들어가기, 공연 순서 보기)만 있었고, 상단
   네비게이션(`LandingNav`)의 앵커 목록에도 `#schedule`이 빠져 있었다. 방문자가 페이지를 끝까지
   스크롤하지 않으면 축제 전체 일정 섹션(6번째 섹션)에 갈 방법이 없었다.
   → `LandingHero`에 "전체 일정 보기"(`#schedule` 앵커) 버튼을 추가하고, `LandingNav`의 앵커
   목록에도 "전체 일정"을 추가했다.
2. **고정 헤더가 앵커 대상 섹션 제목을 가림** — `.landing-nav`는 `position: sticky; top:0; height:64px`
   인데 `.landing-section`에는 `scroll-margin-top`이 전혀 없었다. 즉 `#schedule`, `#booths` 등으로
   점프하면 섹션 제목(고스트 넘버 포함)이 고정 헤더 아래로 가려진 채 시작된다 — 요구사항 §6.1/§9가
   명시적으로 금지하는 상태였다.
   → `.landing-section { scroll-margin-top: 84px }`를 추가했다(헤더 64px + 여유 20px).
3. **축제 전체 일정이 등록 순서로만 표시됨** — `ScheduleItem`에는 순서 필드가 없고 관리자 API도
   `POST /schedule`이 배열 끝에 append만 한다. 실제로 관리자 API에 `11:00` 일정을 추가해보니
   기존 `16:00` 항목 뒤에 붙는 것을 확인했다(§실제 검증 로그, `SECURITY_REVIEW.md` 참고). 방문자
   화면(`LandingSchedule`)은 이 등록 순서를 그대로 렌더링하고 있어 시간 역순으로 보일 수 있었다.
   → `LandingSchedule`에서 `time` 문자열(`HH:MM`, 서버가 항상 이 형식으로 검증) 기준 안정 정렬을
   추가했다. 관리자 `SchedulePage.tsx`는 이미 같은 규칙(`a.time.localeCompare(b.time)`)으로 목록을
   보여주고 있어 규칙을 맞췄다.
4. **공개 사이트에 CSP 헤더 없음** — 관리자 SPA는 컨테이너 nginx가 CSP를 직접 굽는데, 공개
   사이트(`HanRightThat_30`)는 Node 프로세스가 빌드 결과를 직접 서빙하면서도 `securityHeaders`
   미들웨어에 `Content-Security-Policy`가 없었다(디자인 문제라기보다 §6 레이아웃 이전에 확인된
   구조적 결함이라 `SECURITY_REVIEW.md`에도 같이 기록했다).

디자인 톤(다크 배경/파형/EQ/형광 강조색, 섹션별 배경 교차, 절제된 카드 사용, 타이포 계층)은 이미
`요구사항2.md`의 방향과 일치하게 구현되어 있어 색상·타이포를 새로 갈아엎지 않았다. 위 4가지는
"색 몇 개 바꾸기" 수준이 아니라 실제 동선·접근성·데이터 정합성 문제였다.

## 4. 변경 전후 화면 캡처

**이 세션에서는 이 환경에 브라우저 자동화 도구(Playwright/Puppeteer 등)가 설치되어 있지 않고
설치도 하지 않았다** — `node_modules`, PATH 어디에도 없음을 `find`/`which`로 직접 확인했다(아래
`SECURITY_REVIEW.md` §0 참고). 따라서 이번 세션에서 스크린샷 캡처는 수행하지 못했다.

이전 작업의 `DEVELOPMENT.md`에는 "Playwright(headless Chromium)로 데스크톱/모바일을 확인했다"는
서술이 있었으나, 같은 방식으로 실제 실행 흔적(설치된 패키지, 스크린샷 파일, playwright 설정)을
찾을 수 없어 이번 문서·`DEVELOPMENT.md`에서 그 서술을 "실행 근거를 찾지 못함"으로 정정했다. 대신
이번 세션은 실제 API 서버를 기동해 curl로 렌더링에 쓰이는 데이터 계약(정렬·필터링·검증)을
검증했고, 코드를 직접 읽어 CSS 레이아웃(`scroll-margin-top`, 반응형 브레이크포인트, 앵커 링크
목적지)을 확인했다. 실제 브라우저 렌더링 결과(글자 잘림, 실제 대비, 실제 전환 애니메이션)는 이번
세션에서도 시각적으로 확인하지 못했다 — 배포 전 실제 브라우저(데스크톱 1440px, 태블릿 768px,
모바일 390px/360px)로 한 번은 사람이 직접 확인할 것을 권장한다.

## 5. 직접 확인하지 못한 자료

- imweb.me/theme (403으로 접근 불가).
- Figma 원본 디자인(이 세션에 Figma 연결 없음 — 기존 `AGENTS.md`/`요구사항2.md`가 이런 경우
  `tokens.css`/`festival.css`의 기존 톤을 fallback 기준으로 쓰라고 명시하고 있어 그 정책을 따랐다).
- 실제 브라우저 렌더링/스크린샷(§4).
